import { StylesType } from '@/types/mui';

const styles = {
  main: {
    width: '100%',
    display: 'flex',
    gap: 5,
    justifyContent: 'end',
  },
} satisfies StylesType;

export default styles;
